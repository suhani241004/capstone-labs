version: "3"

services:
  backend:
    build: .
    ports:
      - "8000:8000"
    container_name: backend_container

  frontend:
    build: ./client
    ports:
      - "3001:3000"
    container_name: frontend_container
    depends_on:
      - backend